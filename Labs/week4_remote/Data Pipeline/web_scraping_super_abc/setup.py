from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Criacao de uma ETL que ira buscar diariamente os dados dos produtos presentes no ecommerce superabc.com.br',
    author='Pedro Garcias',
)
